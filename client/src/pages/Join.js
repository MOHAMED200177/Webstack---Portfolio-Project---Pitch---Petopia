// src/components/Join.js

import React from 'react';
import PetForm from '../components/PetForm';

const Join = () => {
    return (
        <section id="join">
            <h2>Add a Pet</h2>
            <PetForm />
        </section>
    );
};

export default Join;
